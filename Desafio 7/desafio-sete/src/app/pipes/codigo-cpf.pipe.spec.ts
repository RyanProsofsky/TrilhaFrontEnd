import { CodigoCpfPipe } from './codigo-cpf.pipe';

describe('CodigoCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new CodigoCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
